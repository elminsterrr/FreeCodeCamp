const wydatki = {
  typ: 'Wakacje',
  kwota: 3000
};

// var typ = wydatki.typ;
// var kwota = wydatki.kwota;

const { typ, kwota} = wydatki;

console.log(typ);   // Wakacje
console.log(kwota); // 3000
